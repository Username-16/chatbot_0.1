# This is the main script that runs the chatbot
import chatbot # Import the chatbot module

input_sentence = ()# Example input sentence
output_sentence = chatbot.generate_response(input_sentence) # Generate a response using the chatbot module
print(output_sentence) # Print the output sentence